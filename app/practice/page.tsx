import Studio from "@/components/Studio";

/**
 * Practice mode — the original free-play air studio.
 *
 * It survives the pivot to a duel game because it earns its keep: players use
 * it to warm up, to find their sensitivity settings, and to check their camera
 * is tracking before committing to a match. It is also the only place the
 * non-piano instruments still exist.
 */
export default function PracticePage() {
  return <Studio />;
}
