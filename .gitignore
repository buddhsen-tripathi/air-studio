node_modules
.next
out
build
.DS_Store
*.pem

.env
.env.local
.env*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

next-env.d.ts
tsconfig.tsbuildinfo

# Vendored MediaPipe wasm + model ARE committed on purpose: serving them
# same-origin removes an 8MB cross-origin fetch on every cold load and lets the
# game work offline. Refresh them with `npm run vendor:vision`.
